# X-Digest Environment Variables
# Copy this to .env and fill in your values

# Gemini API key for multimodal digest generation
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# === Delivery (choose one) ===

# WhatsApp (requires OpenClaw with WhatsApp channel configured)
WHATSAPP_RECIPIENT=+1234567890
# Optional: override auto-detected paths (searched via PATH by default)
# OPENCLAW_CLI_PATH=/path/to/openclaw
# OPENCLAW_NODE_PATH=/path/to/node

# Telegram (get a bot token from @BotFather)
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHAT_ID=your-chat-id

# === Tweet Fetching ===

# Bird CLI env file path (defaults to ~/.config/bird/env)
# BIRD_ENV_PATH=~/.config/bird/env

# Optional: override auto-detected bird CLI path (searched via PATH by default)
# BIRD_PATH=/path/to/bird
